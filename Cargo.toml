[package]
name = "screen-brightness"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = { version = "0.31", features = ["default"] }  # 或最新版本
brightness = "0.8"  # 0.8+ 支持 Windows；blocking API